{
  "$schema": "http://json-schema.org/schema",
  "title": "Light",
  "description": "A light source.",
  "type": "array",
  "items": {
    "additionalProperties": false,
    "properties": {
      "name": {
        "description": "Name of the light source.",
        "type": "string",
        "default": "Name"
      },
      "color": {
        "description": "Albedo RGB in decimal format in sRGB primaries.",
        "type": "array",
        "items": {
          "type": "number"
        },
        "default": [0.5, 0.5, 0.5],
        "minItems": 3,
        "maxItems": 3
      },
      "power": {
        "type": "number",
        "default": 0
      },
      "unit": {
        "type": "array",
        "items": {
          "enum": ["lm", "lx", "cd/m2"]
        },
        "minItems": 1,
        "maxItems": 1
      },
      "category": {
        "type": "array",
        "items": {
          "enum": ["Artificial", "Natural"]
        },
        "minItems": 1,
        "maxItems": 1
      },
      "description": {
        "type": "string"
      },
      "sources": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "tags": {
        "description": "Tags",
        "type": "array",
        "items": {
          "type": "string"
        },
        "uniqueItems": true
      }
    },
    "required": [
      "name",
      "color",
      "power",
      "unit",
      "category",
      "description",
      "sources"
    ]
  }
}
